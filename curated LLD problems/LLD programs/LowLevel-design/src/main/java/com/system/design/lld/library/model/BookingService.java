package com.system.design.lld.library.model;

import org.springframework.stereotype.Service;

@Service
public class BookingService {
    public boolean makeBooking(Book book) {
        return true;
    }
}
